[pytest]
# iOS interop tests want verbose logging because each test reads two
# subsystems' log streams (iOS diag.log + Sideband LXMRouter tap).
addopts = -v --tb=short
testpaths = .
python_files = test_*.py
log_cli = true
log_cli_level = INFO
